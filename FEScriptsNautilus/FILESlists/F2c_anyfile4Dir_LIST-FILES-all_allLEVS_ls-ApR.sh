#!/bin/sh
##
## SCRIPT: F2c_anyfile4Dir_LIST_FILESdirs-all_allLEVS_ls-ApR.sh
##
## PURPOSE: List ALL files in a directory AND in its
##          subdirectories at ALL levels below, using 'ls'.
##
## METHOD:  Puts the output of the 'ls' command in a text file.
##
##          Shows the text file using a text-file viewer of the user's
##          choice.
##
## HOW TO USE: In Nautilus, select any file in the 'navigated to' directory.
##             Then right-click and choose this Nautilus script to run.
##
## Created: 2010mar23 Based on 00_list_allDirFILES_recursive_find.sh
## Changed: 2012feb29 Changed the script name in the comment above.

## FOR TESTING: (show statements as they execute)
# set -x

#################################################
## Prepare the output file.
##
## If the user has write-permission on the
## current directory, put the file in the pwd.
## Otherwise, put the file in /tmp.
#################################################

CURDIR="`pwd`"

OUTFILE="${USER}_temp_dirAllFILESrecursiveList_ls-Ap.txt"

if test ! -w "$CURDIR"
then
   OUTFILE="/tmp/$OUTFILE"
fi

if test -f "$OUTFILE"
then
   rm -f "$OUTFILE"
fi


#####################################
## Generate a heading for the listing.
#####################################

DATETIME=`date '+%Y %b %d  %a  %T%p'`

echo "\
..................... $DATETIME ............................

List of FILES (and DIRECTORIES) under directory

  $CURDIR

--- ALL levels (recursive).

Includes 'hidden' files.

...........................................................................
" >  "$OUTFILE"


#######################################
## Add the 'ls' output to the listing.
#######################################

# ls -A -p -R --group-directories-first >> "$OUTFILE"

ls -A -p -R >> "$OUTFILE"


#####################################
## Add a trailer to the listing.
#####################################

SCRIPT_BASENAME=`basename $0`
SCRIPT_DIRNAME=`dirname $0`

echo "\
...........................................................................
 
This list was generated by script
   $SCRIPT_BASENAME
in directory
   $SCRIPT_DIRNAME

Used command

     ls -A -p -R

'-A' requests that hidden files (and directories) be shown ---
other than '.' and '..'.

'-p' requests that directories be indicated by an ending slash (/).

..................... $DATETIME ............................
" >>  "$OUTFILE"


######################
## Show the list.
######################

## . $HOME/.gnome2/nautilus-scripts/.set_VIEWERvars.shi

. $HOME/.freedomenv/feNautilusScripts/set_DIR_NautilusScripts.shi
. $DIR_NautilusScripts/.set_VIEWERvars.shi

$TXTVIEWER "$OUTFILE" &
